SERVICE DROPDOWN EMPTY AFTER SELECTING TOWN

Cause:
Astro escaped JSON inside <script type="application/json">.

Fix:
Use:
set:html={JSON.stringify(data)}

Result:
Browser can correctly parse JSON and rebuild service dropdown.

Cloudflare build failures due to folder casing (Towns vs towns)

Backup .astro files causing build errors

Logo not loading due to filename case mismatch

Deployment workflow confirmed (git push → Cloudflare build)

Keeping this log prevents repeating debugging work later.

Header logo appeared too small due to whitespace in PNG.

Long website URLs overflowed card container.

Initial confusion about CompanyList.astro vs ma/[slug]/[service].astro.